import React from 'react'
/* const handleManageAttendance = async (eventId) => {
    try {
        const response = await axios.get(`http://localhost:5000/api/events/${eventId}/attendees`);
        console.log("Attendees:", response.data);
      } catch (error) {
        console.error("Failed to fetch attendees:", error);
      } 
    }; */

    /*()=>handleManageAttendance(event.id) */
function AttendeeManagment() {
  return (
    <div>AttendeeManagment</div>
  )
}

export default AttendeeManagment;